id: control-rule-based-eu
display_name: "Control — rule-based gainers (EU)"
description: |
  EU counterpart of control-rule-based. Buys top-5 highest previous-day
  gainers from the eu_blue_chips universe (DAX 40 + CAC 40 + AEX 25,
  ~100 names). Shadow tier only — Alpaca does not trade EU shares.
  Baseline that every EU LLM strategy must beat.

implementation: rule_based

active: true
tier: shadow
region: uk-eu

capital_gbp: 10000
max_positions: 5
max_position_pct: 30
min_position_gbp: 50

use_stops: false
use_take_profits: false

universe: eu_blue_chips
