id: regime-router
display_name: Macro aligned (variant) (variant)
description: "Auto-spawned variant of macro-aligned-hmm on 2026-06-13 by the weekly\
  \ evolution agent. Reason: HarbourFront study demonstrates cross-asset HMM regime\
  \ routing (yield curve + gold/equity + Bitcoin) achieves Sharpe 1.18 under walk-forward\
  \ evaluation, with cross-asset features outperforming traditional technicals. This\
  \ variant breaks the exposure axis: it deploys exclusively into one signal mode\
  \ per session (mean-reversion OR momentum, never both) rather than the parent's\
  \ always-on cross-sectional picking \u2014 the regime gate is the primary filter,\
  \ stock selection is secondary."
implementation: llm
active: true
capital_gbp: 10000
max_positions: 2
max_position_pct: 30
min_position_gbp: 50
use_stops: true
use_take_profits: true
stop_loss_pct: -4.0
take_profit_pct: 5.0
tools:
- get_universe
- get_history
- get_technicals
- get_recent_news
- get_sector_strength
- get_macro_view
- get_daily_news_brief
model_assignment:
  wide_scoring: haiku
  deep_analysis: sonnet
  final_select: sonnet
runs_in:
- region: us
  tier: shadow
  universe: t212_isa_us
  alpaca_slot: null
- region: uk-eu
  tier: shadow
  t212_slot: null
  universe: t212_isa_uk_eu
  alpaca_slot: null
skip_if_earnings_in_days: 1
prefilter_mode: python
prefilter_top_n: 100
prefilter_sort_key: dollar_volume_desc
last_tune_date: '2026-05-27'
cost_gate_multiplier: 2.5
